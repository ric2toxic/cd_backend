ALTER TABLE `oc_product` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '0' COMMENT '0=disabled, 1= active, 2=have to moderate, 3=seller disabled so no need to moderate untiil he changed status to 2';